# Changesets

Every change to a published package needs a changeset. Run `yarn changeset`,
pick the package and the bump, and write one or two sentences saying what
changed from the point of view of somebody using the package.

The files in this directory are consumed and deleted at release time.
